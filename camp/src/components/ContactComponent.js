import { React } from 'react';

class Contact extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return(
            <div>Contact</div>
        );
    }
}

export default Contact;